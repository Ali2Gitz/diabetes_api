# Diabetes Prediction API
A FastAPI service that uses a Random Forest model to predict diabetes risk based on the Pima Indians dataset.

## How to use
- **Endpoint:** `POST /predict`
- **Input:** JSON with 8 health markers (Glucose, BMI, etc.)

## Deployment
Deployed on Render.